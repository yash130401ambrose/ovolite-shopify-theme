<section class="scroll-divider-section">
  <!-- Split background -->
  <div class="scroll-bg-top"></div>
  <div class="scroll-bg-bottom"></div>

  <!-- Curved ribbon with embedded text -->
  <svg class="scroll-divider-svg" viewBox="0 0 1600 300" preserveAspectRatio="xMidYMid slice">
    <!-- Wider curved ribbon band -->
    <path id="ribbon-path" fill="{{ section.settings.ribbon_color }}"
      d="M0,160 Q800,80 1600,160 L1600,200 Q800,120 0,200Z" />

    <!-- Text path, centered within the ribbon -->
    <path id="text-path" fill="none" d="M0,165 Q800,90 1600,165" />

    <text class="scroll-divider-text" fill="{{ section.settings.text_color }}" font-size="{{ section.settings.font_size }}">
      <textPath href="#text-path" startOffset="50%">
        {{ section.settings.text }} • {{ section.settings.text }} • {{ section.settings.text }}
      </textPath>
    </text>
  </svg>
</section>

<style>
.scroll-divider-section {
  position: relative;
  overflow: hidden;
  width: 100%;
  height: 260px;
}

/* Split background colors */
.scroll-bg-top,
.scroll-bg-bottom {
  position: absolute;
  width: 100%;
  height: 50%;
  left: 0;
  z-index: 0;
}

.scroll-bg-top {
  top: 0;
  background: {{ section.settings.top_bg_color }};
}

.scroll-bg-bottom {
  bottom: 0;
  background: {{ section.settings.bottom_bg_color }};
}

/* SVG layer */
.scroll-divider-svg {
  width: 100%;
  height: 260px;
  position: relative;
  z-index: 2;
}

/* Text styling */
.scroll-divider-text {
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 2px;
  white-space: nowrap;
  text-anchor: middle;
  dominant-baseline: middle;
}

@media (max-width: 768px) {
  .scroll-divider-text {
    font-size: calc({{ section.settings.font_size }}px * 0.7);
  }
}
</style>

<!-- GSAP scroll animation -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
<script>
document.addEventListener("DOMContentLoaded", () => {
  gsap.registerPlugin(ScrollTrigger);
  const text = document.querySelector(".scroll-divider-text textPath");

  gsap.to(text, {
    attr: { startOffset: "150%" }, // left → right scroll
    ease: "none",
    scrollTrigger: {
      trigger: ".scroll-divider-section",
      start: "top bottom",
      end: "bottom top",
      scrub: true
    }
  });
});
</script>

{% schema %}
{
  "name": "Curved Scroll Divider ",
  "settings": [
    {
      "type": "text",
      "id": "text",
      "label": "Divider Text",
      "default": "IT’S A MATCH(A)"
    },
    {
      "type": "range",
      "id": "font_size",
      "label": "Font Size",
      "min": 20,
      "max": 120,
      "default": 72
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#A5D6A7"
    },
    {
      "type": "color",
      "id": "ribbon_color",
      "label": "Ribbon Color",
      "default": "#204C2A"
    },
    {
      "type": "color",
      "id": "top_bg_color",
      "label": "Top Background Color",
      "default": "#E5EED8"
    },
    {
      "type": "color",
      "id": "bottom_bg_color",
      "label": "Bottom Background Color",
      "default": "#DCE8C4"
    }
  ],
  "presets": [
    {
      "name": "Curved Scroll Divider (Embedded Ribbon)"
    }
  ]
}
{% endschema %}
